{{#if withinArea}}
  {{#if canEditor}}
    {{field/mdi-range title=rating.factor.name negative=rating.factor.is_negative value=rating.score}}
  {{else}}
    {{field/mdi-progress title=rating.factor.name negative=rating.factor.is_negative value=rating.score}}
  {{/if}}
{{/if}}
