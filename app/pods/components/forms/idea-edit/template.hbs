<form class="col s12 m8 offset-m2" {{action 'save' on='submit'}}>
  <div class="card">
    <div class="card-content">
      <span class="card-title grey-text text-darken-4">
        {{title}}
      </span>

      <div class="row">
        <div class="input-field col s12">
          {{input id='name' name='name' placeholder='Introduce your idea with a catchy title.' value=model.name}}
          <label class="active" for="name">Idea title</label>
        </div>
      </div>

      <div class="row">
        <div class="input-field col s12">
          {{field/redactor-editor inputId='description' placeholder='Add something witty.' value=model.description}}
          <label class="active" for="description">Description</label>
        </div>
      </div>
    </div>

    <div class="card-action">
      <button type='submit' class='col s12 l2 waves-effect waves-light btn text-white pink lighten-2'>Save</button>
      <button {{action 'cancel'}} class='col s12 l2 waves-effect waves-light btn text-white'>Close</button>
    </div>
  </div>
</form>
