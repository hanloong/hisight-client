{{#each areas as |area|}}
  <div class="card">
    <div class="card-content">
      <h5 class="center-align">{{area.name}}</h5>
      {{#each ratings as |rating|}}
        {{field/idea-rating rating=rating area=area factor=rating.factor canEdit=canEdit}}
      {{/each}}
    </div>
  </div>
{{/each}}
